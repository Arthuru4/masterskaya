import React from "react";
import s from "./Item.module.scss";

interface IProps {
  fill?:string
}

const SVG = (props: IProps) => {
    return (
      <div className={s.Item}>
          
      </div>
    );
  };

export default SVG;
